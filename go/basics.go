package main

import (
	"fmt"
	// "reflect"
	// "sort"
)

func basics() {
	fmt.Println("helloo")

	// var name string = "Hello"
	// var lang string = "golang"
	// var isTrue bool = true
	// var num int = 9
	// var num1 = 9
	// shortHand := "values"
	// var values uint
	// values = 999
	// values = 999.99 //shows error

	// const pi = 3.14

	// const (
	// 	host = "localhost"
	// 	port = 3000
	// )

	// var (
	// 	v1 = "one"
	// 	v2 = "two"
	// )

	// name := []uint{1, 2, 3, 4, 5}
	// fmt.Println(name)
	// fmt.Println(reflect.TypeOf(name))

	// i := 42
	// f := float64(i)
	// u := uint(f)
	// fmt.Println(reflect.TypeOf(i), reflect.TypeOf(f), reflect.TypeOf(u))

	// name1 := "golang"
	// fmt.Println("What is your name?")
	// fmt.Printf("My name is %s", name1)

	// percent := (7.0 / 9) * 100
	// fmt.Printf("%f", percent)

	// one("hello")

	// func1 := func(v1 string) {
	// 	fmt.Println(v1)
	// }

	// func1("helsdedaslo")

	// func2 := func(v1 string) (string, string) {
	// 	return v1 + "hello", "this is"
	// }
	// i, j := func2("kjsdjnsjnjdfbnkjndbsjbjsbdkjbfjsbndjf")
	// fmt.Println(i, j)

	// func() {
	// 	fmt.Println("self")
	// }()

	// numbers := []int{5, 2, 9, 1, 7}

	// sort.Slice(numbers, func(i, j int) bool {
	// 	return numbers[i] < numbers[j]
	// })
	// fmt.Println(numbers)

	// result1 := calculate(40, 2, func(x, y int) int {
	// 	return x + y
	// })
	// result2 := calculate(40, 2, func(x, y int) int {
	// 	return x - y
	// })
	// fmt.Println(result1)
	// fmt.Println(result2)

	// ans := counter()
	// fmt.Println(ans())
	// fmt.Println(ans())

}

// func one(v1 string) {
// 	fmt.Println(v1)
// }

// func calculate(a, b int, f func(x, y int) int) int {
// 	return f(a, b)
// }

// func counter() func() int {
// 	count := 0
// 	return func() int {
// 		count++
// 		return count
// 	}
// }
